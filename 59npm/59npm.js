console.log("This is 59npm.js");

// npm Notes

// Use npm init to create package.json
// Use npm install package_name to install
// Use npm uninstall package_name to install

//// or u can use only i instead of install

// if u accidentally delete node modules then u can use the command npm (install or i) to install all the dependencies in the package.json

// if u want to install package of a specific version then u can use the following code
// npm install slugify@1.3.5
// here the version code is major.minor.patch
// (major update/version)/(minor updates like function update)/(bug fixes)

// dev dependencies
// dependencies only required while developing
// npm install nodemon --save-dev

// Global dependencies
// to install a package globally for this computer
// npm install nodemon --global

//// in the dependencies u can use =>
// ~ instead of ^
// > to upgrade version to latest